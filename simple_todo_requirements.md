# シンプルToDoアプリ 要件定義

## 1. アプリ概要

本アプリは、Android端末内で個人のタスクを管理するシンプルなToDoアプリとする。

タスク登録時の入力負担を最小限にしつつ、カテゴリ分類、期限、詳細メモ、サブタスクなどを必要に応じて追加できる設計とする。

操作はタップ、スワイプ、ドラッグ＆ドロップなどを活用し、説明を読まなくても直感的に利用できることを重視する。

---

## 2. 基本方針

- Android専用アプリとする
- 会員登録やログインは設けない
- データはAndroid端末内にのみ保存する
- 外部サーバーへのデータ送信やクラウド同期は行わない
- タスク名だけで素早く登録できる
- 詳細な情報は必要な場合だけ追加できる
- 日常的な操作を少ないタップ数で完了できる
- ライトモードとダークモードを簡単に切り替えられる

---

## 3. タスク登録機能

### 3.1 クイック登録

- タスク名だけで新しいタスクを登録できる
- タスク名以外の項目は入力必須としない
- タスク名を入力後、キーボードまたは登録ボタンから即座に登録できる
- タスク登録時に詳細入力画面を経由しなくてもよい
- 連続して複数のタスクを登録しやすい操作にする

### 3.2 タスクの基本情報

各タスクは、以下の情報を保持できる。

- タスク名
- 詳細メモ
- カテゴリ
- 期限
- 完了状態
- 並び順
- サブタスク
- 作成日時
- 更新日時
- 完了日時

タスク名のみ必須とし、それ以外は任意項目とする。

---

## 4. カテゴリ管理機能

### 4.1 カテゴリマスタ

- ユーザーが任意のカテゴリを事前登録できる
- 登録済みカテゴリを編集できる
- 登録済みカテゴリを削除できる
- カテゴリの表示順を変更できる
- カテゴリごとに識別しやすい表示を設定できる

カテゴリの識別方法として、以下を候補とする。

- カテゴリ名
- カテゴリカラー
- アイコン

### 4.2 タスクへのカテゴリ設定

- 1つのタスクに1つのカテゴリを設定できる
- カテゴリは未設定でもよい
- タスク登録後でもカテゴリを変更できる
- タスク一覧上から直感的な操作でカテゴリを設定できる
- タスク詳細画面を開かなくてもカテゴリ変更を完了できることが望ましい

カテゴリ設定の操作候補は、以下とする。

- カテゴリアイコンのタップ
- スワイプ操作
- 長押しメニュー
- カテゴリ欄へのドラッグ＆ドロップ

---

## 5. タスク詳細機能

### 5.1 詳細メモ

- タスクごとに任意の詳細メモを登録できる
- 詳細メモは複数行のテキスト入力に対応する
- 詳細メモは後から編集および削除できる
- 詳細メモが存在するタスクは、一覧上で判別できるようにする

### 5.2 期限

- タスクごとに任意の期限を設定できる
- 期限として日付を設定できる
- 期限は後から変更および解除できる
- 期限が設定されているタスクは、一覧上で期限を確認できる
- 期限切れの未完了タスクを視覚的に判別できるようにする

初期リリースでは、期限時刻や通知は必須要件に含めない。

---

## 6. タスク完了機能

- 未完了タスクを直感的な操作で完了状態に変更できる
- 完了済みタスクを未完了状態に戻せる
- 完了時には完了日時を記録する
- 完了操作に対して、視覚的または触覚的なフィードバックを表示する

完了操作の候補は、以下とする。

- チェックボックスのタップ
- タスクのスワイプ
- タスクの長押し操作

誤操作を防ぐため、完了直後に取り消し操作を提供することが望ましい。

---

## 7. 完了済みタスクの表示切り替え

- タスク一覧で完了済みタスクを表示または非表示にできる
- 表示切り替えは一覧画面から少ない操作で行える
- 完了済みタスクを表示する場合、未完了タスクと視覚的に区別する
- 完了済みタスクは削除せず、端末内に履歴として保持する
- 表示設定はアプリを終了しても維持する

完了済みタスクの表示方法は、以下を候補とする。

- 一覧下部にまとめて表示
- 完了済みセクションとして折りたたんで表示
- 表示切り替えボタンで一覧全体への表示・非表示を変更

---

## 8. タスク並び替え機能

- タスク一覧の表示順をユーザーが変更できる
- ドラッグ＆ドロップによって直感的に並び替えられる
- 変更した並び順は端末内に保存する
- アプリを再起動しても並び順を維持する
- サブタスクについても同様に並び替えられる

並び替え中であることが明確に分かる表示とし、通常のスクロール操作と混同しにくい操作設計にする。

---

## 9. サブタスク機能

### 9.1 サブタスク登録

- 1つのタスクに対して複数のサブタスクを登録できる
- サブタスクは、タスクを完了するための複数のステップとして扱う
- サブタスク名を入力して素早く追加できる
- サブタスクを編集および削除できる
- サブタスクの並び順を変更できる

### 9.2 サブタスク完了

- サブタスクごとに完了状態を管理できる
- 完了済みサブタスクを未完了に戻せる
- 親タスクの完了状態とサブタスクの完了状態は個別に保持する
- サブタスクの完了状況を親タスク上で確認できる

表示例は以下とする。

- 「2 / 5 完了」
- 進捗バー
- 完了済み件数と総件数

親タスクを完了した際に、未完了のサブタスクを自動完了にはしない方針を基本とする。

---

## 10. サブタスクの折りたたみ・展開

- タスク一覧上でサブタスクを折りたたみまたは展開できる
- 折りたたみと展開はワンタップで切り替えられる
- サブタスクが存在するタスクには、展開可能であることが分かるアイコンを表示する
- 折りたたみ中でもサブタスクの総数や完了状況を確認できる
- タスクごとの折りたたみ状態を保持する

折りたたみ状態では、タスク一覧の情報量を抑え、一覧性を維持する。

---

## 11. 今日のタスク表示機能

- 今日やるべきタスクだけに絞り込んで表示できる
- 一覧画面から簡単に「今日」表示へ切り替えられる
- 通常の全タスク表示へ簡単に戻せる

「今日やるべきタスク」には、以下を含める。

- 期限が今日に設定されている未完了タスク
- 期限を過ぎている未完了タスク

期限が設定されていないタスクは、原則として「今日」表示には含めない。

画面構成として、以下を候補とする。

- 「すべて」と「今日」のタブ切り替え
- 一覧上部のフィルターボタン
- ボトムナビゲーションによる切り替え

---

## 12. データ保存要件

- すべてのデータをAndroid端末内に保存する
- インターネット接続がない状態でも、すべての主要機能を利用できる
- 外部サーバーやクラウドストレージは使用しない
- 会員登録やログイン機能は実装しない
- アプリ終了後および端末再起動後もデータを保持する
- タスクの追加、編集、削除、完了、並び替えを即座に保存する

保存対象には、以下を含める。

- タスク
- サブタスク
- カテゴリマスタ
- タスクの並び順
- サブタスクの並び順
- 折りたたみ状態
- 完了済みタスクの表示設定
- テーマ設定

初期リリースでは、複数端末間の同期や独自のクラウドバックアップは対象外とする。

---

## 13. テーマ切り替え機能

- ライトモードとダークモードを提供する
- 主要画面から常にワンタップでテーマを切り替えられる
- 設定画面を開かなくても切り替えられる
- 切り替え結果を即座に画面全体へ反映する
- 選択したテーマを端末内に保存する
- アプリを再起動しても選択状態を維持する

テーマ切り替えボタンは、タスク一覧画面のヘッダーなど、常にアクセスしやすい場所に配置する。

初期要件では、端末のシステム設定への自動追従は必須とせず、ユーザーがライトまたはダークを明示的に選択する方式とする。

---

## 14. 想定画面

初期リリースでは、以下の画面を基本構成とする。

### 14.1 タスク一覧画面

- タスクのクイック登録
- すべて／今日の表示切り替え
- 完了済みタスクの表示・非表示
- タスクの完了操作
- タスクの並び替え
- サブタスクの折りたたみ・展開
- カテゴリの設定・変更
- ライトモード・ダークモードの切り替え

### 14.2 タスク編集画面

- タスク名の編集
- 詳細メモの入力
- 期限の設定
- カテゴリの設定
- サブタスクの追加・編集・削除
- タスクの削除

### 14.3 カテゴリ管理画面

- カテゴリの新規登録
- カテゴリ名の編集
- カテゴリの削除
- カテゴリカラーまたはアイコンの設定
- カテゴリの並び替え

---

## 15. 初期リリースの対象外

シンプルさを維持するため、以下は初期リリースの対象外とする。

- 会員登録、ログイン
- 独自バックエンド
- クラウド同期
- 複数ユーザーでの共有
- タスクへの担当者設定
- コメント機能
- タグ機能
- 複数カテゴリの設定
- 通知、リマインダー
- 繰り返しタスク
- 画像やファイルの添付
- カレンダー表示
- 統計、分析機能
- AIによるタスク生成や分類
- iOS版、Web版
- サブタスクの多階層化

サブタスクは、親タスク直下の1階層のみとする。

---

## 16. 主要な操作要件

ユーザーが頻繁に行う操作について、以下を目標とする。

- タスク登録：タスク名入力後、1操作で完了
- タスク完了：1操作で完了
- カテゴリ変更：一覧画面から1〜2操作で完了
- サブタスクの展開・折りたたみ：1操作で完了
- 今日のタスク表示：1操作で切り替え
- 完了済みタスクの表示切り替え：1操作で切り替え
- テーマ切り替え：1操作で完了
- タスク並び替え：ドラッグ＆ドロップで完了

---

## 17. 非機能要件

### 操作性

- 初回起動時から説明なしでも基本操作を理解できる
- 主要な操作をタスク一覧画面内で完結できる
- 片手でも操作しやすいレイアウトにする
- 操作結果をアニメーションや触覚フィードバックで伝える
- 誤操作を取り消せる仕組みを可能な範囲で設ける

### パフォーマンス

- アプリ起動後、すぐにタスク一覧を表示する
- タスクの追加や完了を即座に画面へ反映する
- タスク数が増えても一覧を滑らかにスクロールできる
- 端末内データの保存処理によって操作を待たせない

### プライバシー

- タスクデータを外部へ送信しない
- インターネット権限を必要としない構成を基本とする
- ユーザーの行動データやタスク内容を収集しない
- アプリ削除時には、原則としてアプリ内データも削除される
